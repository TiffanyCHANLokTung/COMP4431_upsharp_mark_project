(function(imageproc) {
    "use strict";

    imageproc.unsharp = function(inputImage, outputImage, amount, radius, threshold) {

        var kernel = []; // Gaussian kernel
        // sigma
        // kernel size

        // Apply gaussian kernel



    }
}(window.imageproc = window.imageproc || {}));
